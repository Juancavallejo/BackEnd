    <div class="mb-5">
      <div class="d-flex justify-content-end border-bottom border-dark border-2">
        <h2 class="m-3"> Bienvenido {{user}} al servidor</h2>
        <a href="/logout" class="btn btn-secondary m-3">Logout</a>
      </div>
    </div>
   {{> form}}
   {{> data}}
